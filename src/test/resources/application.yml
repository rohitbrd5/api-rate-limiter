server:
  port: 0  # Random port for tests

spring:
  application:
    name: api-rate-limiter-test

# Rate Limiting Configuration (for tests)
rate-limit:
  # Default limits for unknown clients
  default-limit: 60
  default-window: seconds

  # Inactive client cleanup configuration
  cleanup:
    idle-timeout: 50   # 50 milliseconds for faster tests
    sweep-interval: 60000   # 1 minute (in milliseconds)

  # Per-client rate limit configurations
  clients:
    customerA:
      limit: 100
      windowType: minutes
    customerB:
      limit: 1000
      windowType: minutes
    customerC:
      limit: 10
      windowType: seconds
    burstClient:
      limit: 5
      windowType: seconds
    customerD:
      limit: 5
      windowType: seconds
    # Client for testing rate limit exhaustion (low limit so sequential requests can exhaust it)
    exhaustTestClient:
      limit: 5
      windowType: seconds

# Spring Boot Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info

# Swagger/OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.example.ratelimiter: WARN  # Reduce log noise in tests
